# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# There is deliberately no node_modules entry that matters here: this repo has
# no dependencies and no build step, and that is the point of it. If a
# package.json ever appears, ask what it is buying first.
node_modules/
